# OCaml / dune
_build/
_opam
_opam/
*.install

# Generated JS bundles (produced by scripts/build-web.sh)
prototypes/*/public/*.js

# Agda interface files (_build/ is already covered above)
*.agdai

# Editor / OS
.DS_Store
*.swp

# Claude Code local state (plans, auto-memory, caches — not for version control)
.claude/
